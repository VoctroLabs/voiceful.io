// Define common imports here

const action = mobx.action;
const autobind = CoreDecorators.autobind;
const autorun = mobx.autorun;
const computed = mobx.computed;
const observable = mobx.observable;
const observer = mobxReact.observer;
const reaction = mobx.reaction;
const runInAction = mobx.runInAction;
const syllable = require('syllable');

const CSSTransition = ReactTransitionGroup.CSSTransition;
